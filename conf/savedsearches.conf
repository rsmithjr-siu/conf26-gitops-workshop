[My Saved Search]
# Trailing space removed so this merge should work
search = index=_internal | stats count by sourcetype
cron_schedule = */5 * * * *
dispatch.earliest_time = -15m
dispatch.latest_time = now
enableSched = 1
